package ${packageName}.mapper;

import org.mass.framework.core.mapper.BaseMapper;
import ${packageName}.bean.${className};

public interface ${className}Mapper extends BaseRepository<${className}> {
	
	
}
